import React from 'react';
import './header.scss';

function Header() {
    return (
        <header className="app-header">
            <h3 className="title">Format your Digit in many ways </h3>
        </header>
    );
}

export default Header;
